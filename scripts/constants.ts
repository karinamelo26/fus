import { join } from 'path';

export const DIST_PATH = join(process.cwd(), 'dist');
export const ELECTRON_PATH = join(process.cwd(), 'electron');
export const DIST_ELECTRON_PATH = join(process.cwd(), 'dist', 'electron');
